import styled, { css } from "styled-components";

export const WebPlayer = styled.div`
  ${({ theme }) => css`
    width: 100%;
    height: 100%;
    display:flex;
    flex-direction:column;

    } */
  `}
`;
